## `pages/gallery_page.dart`

- **目的**: ローカルに保存されたタグ付け済みの画像ギャラリーを表示します。
- **主要機能**:
    - `LocalStorageService`を通じて、タグ付けされた画像の情報を読み込みます。
    - 読み込んだ画像をグリッド状に表示します。
    - 各画像には、関連付けられたタグがオーバーレイで表示されます。
    - 「画像にタグ付け」ボタンを配置し、押下するとタグ付け画面(`/tag-lens`)に遷移します。
